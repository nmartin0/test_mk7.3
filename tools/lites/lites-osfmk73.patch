diff --git a/conf/gensym.awk b/conf/gensym.awk
index cb657a0..0c8bae2 100644
--- a/conf/gensym.awk
+++ b/conf/gensym.awk
@@ -40,7 +40,7 @@ BEGIN {
 
 # Take an arbitrarily complex C symbol or expression and constantize it.
 /^expr/ {
-	print "asm (\"";
+	printf "asm (\"\\n";
 	if ($3 == "")
 		printf "* %s mAgIc%%0\" : : \"i\" (%s));\n", $2, $2;
 	else
@@ -49,14 +49,14 @@ BEGIN {
 
 # Output a symbol defining the size of a C structure.
 /^size/ {
-	print "asm (\"";
+	printf "asm (\"\\n";
 	printf "* %s_SIZE mAgIc%%0\" : : \"i\" (sizeof(struct %s)));\n",
 		toupper($3), $2;
 }
 
 # Output a symbol defining the byte offset of an element of a C structure.
 /^offset/ {
-	print "asm (\"";
+	printf "asm (\"\\n";
 	if ($5 == "")
 	{
 		printf "* %s_%s mAgIc%%0\" : : \"i\" (&((struct %s*)0)->%s));\n",
diff --git a/conf/newvers.sh b/conf/newvers.sh
index 840cb30..fa0a63d 100644
--- a/conf/newvers.sh
+++ b/conf/newvers.sh
@@ -30,11 +30,11 @@ fi
 CONFIG=`expr "$d" : '.*/\([^/]*\)$'`
 d=`expr "$d" : '.*/\([^/]*/[^/]*\)$'`
 (
-  echo "char ostype[] = \"Lites\";" ;
-  echo "char osrelease[] = \"${version}\";" ;
-  echo "char version[] = \"${name} ${v}: ${t}; $config ($h)\\n\";" ;
-  echo "char version_short[] = \"${version} ${config}\\n\";" ;
-  echo "char cmu_copyright[] = \"\\" ;
+  printf "%s\n" "char ostype[] = \"Lites\";" ;
+  printf "%s\n" "char osrelease[] = \"${version}\";" ;
+  printf "%s\n" "char version[] = \"${name} ${v}: ${t}; $config ($h)\\n\";" ;
+  printf "%s\n" "char version_short[] = \"${version} ${config}\\n\";" ;
+  printf "%s\n" "char cmu_copyright[] = \"\\" ;
   sed <$copyright -e '/^#/d' -e 's;[ 	]*$;;' -e '/^$/d' -e 's;$;\\n\\;' -e 's/"/\\"/g';
   echo "\";";
 ) > vers.c
diff --git a/include/i386/stdarg.h b/include/i386/stdarg.h
index 1110b66..6fabd7c 100644
--- a/include/i386/stdarg.h
+++ b/include/i386/stdarg.h
@@ -36,23 +36,31 @@
 #ifndef _STDARG_H_
 #define	_STDARG_H_
 
-typedef char *va_list;
-
-#define	__va_promote(type) \
-	(((sizeof(type) + sizeof(int) - 1) / sizeof(int)) * sizeof(int))
-
-#define	va_start(ap, last) \
-	(ap = ((char *)&(last) + __va_promote(last)))
-
-#ifdef KERNEL
-#define	va_arg(ap, type) \
-	((type *)(ap += sizeof(type)))[-1]
-#else
-#define	va_arg(ap, type) \
-	((type *)(ap += sizeof(type) < sizeof(int) ? \
-		(abort(), 0) : sizeof(type)))[-1]
-#endif
+/*
+ * AI-ONLY NOTE: use the compiler's own varargs builtins.
+ *
+ * The original definitions computed the argument pointer by taking the
+ * address of the last named parameter and stepping past it:
+ *
+ *	typedef char *va_list;
+ *	#define va_start(ap, last) (ap = ((char *)&(last) + __va_promote(last)))
+ *
+ * That assumes a stack layout the compiler is not obliged to provide.
+ * Modern GCC at -O2 does not, and the result is that named parameters
+ * read correctly while every variadic argument is garbage. The symptom
+ * here was printf printing a valid string pointer as the bytes of an
+ * unrelated function -- "panic: UWVS..." where UWVS is the i386
+ * prologue push ebp, push edi, push esi, push ebx -- while a format
+ * string with no arguments printed perfectly.
+ *
+ * __builtin_va_list and friends are what GCC uses for its own <stdarg.h>
+ * and are correct for whatever calling convention and optimisation
+ * level are in effect.
+ */
+typedef __builtin_va_list va_list;
 
-#define	va_end(ap)
+#define	va_start(ap, last)	__builtin_va_start((ap), (last))
+#define	va_arg(ap, type)	__builtin_va_arg((ap), type)
+#define	va_end(ap)		__builtin_va_end(ap)
 
 #endif /* !_STDARG_H_ */
diff --git a/include/sys/exec_file.h b/include/sys/exec_file.h
index 464f3ee..6659637 100644
--- a/include/sys/exec_file.h
+++ b/include/sys/exec_file.h
@@ -45,13 +45,23 @@ typedef enum binary_type {
     } binary_type_t;
 
 /* Names for the above (for @bin expansion) */
+/*
+ * The ## operator pastes preprocessing tokens, and two string literals
+ * cannot be pasted into one -- modern compilers reject it with
+ * "pasting \"i386_\" and \"bad\" does not give a valid preprocessing
+ * token". Older preprocessors tolerated it.
+ *
+ * None is needed: adjacent string literals are concatenated by the
+ * compiler, so m "bad" with m as "i386_" yields "i386_bad" exactly as
+ * intended.
+ */
 #define ATSYS_NAMES(m) \
-    m ## "bad", m ## "lites", m ## "lites", m ## "lites", m ## "lites", \
-    m ## "lites", \
-    m ## "bnr", m ## "netbsd", m ## "freebsd", m ## "ux", \
-    "script", m ## "isc4", m ## "linux", m ## "linux", m ## "linux", \
-    m ## "ultrix", m ## "riscos", m ## "hpbsd", m ## "hpux", m ## "hpkludge", \
-    m ## "hpelf", m ## "osf1"
+    m "bad", m "lites", m "lites", m "lites", m "lites", \
+    m "lites", \
+    m "bnr", m "netbsd", m "freebsd", m "ux", \
+    "script", m "isc4", m "linux", m "linux", m "linux", \
+    m "ultrix", m "riscos", m "hpbsd", m "hpux", m "hpkludge", \
+    m "hpelf", m "osf1"
 
 union exec_data {
 	unsigned short	short_magic;
diff --git a/server/kern/kern_sig.c b/server/kern/kern_sig.c
index f131504..057d880 100644
--- a/server/kern/kern_sig.c
+++ b/server/kern/kern_sig.c
@@ -1064,11 +1064,11 @@ run:
 		 */
 		SD(printf("%8x[%d]: psignalDIRECT %x[%d] %x\n",curproc,curproc->p_pid, p,p->p_pid, signum));
 		switch ((integer_t)p->p_sigacts->ps_sigact[signum]) {
-		      case SIG_IGN:
-		      case SIG_HOLD:
+		      case (integer_t)SIG_IGN:
+		      case (integer_t)SIG_HOLD:
 			break;
 
-		      case SIG_DFL:
+		      case (integer_t)SIG_DFL:
 			sig_default(p, signum);
 			break;
 
@@ -1167,7 +1167,7 @@ int issignal(p)
 		 */
 		switch ((integer_t)p->p_sigacts->ps_sigact[signum]) {
 
-		case SIG_DFL:
+		case (integer_t)SIG_DFL:
 			/*
 			 * Don't take default actions on system processes.
 			 */
@@ -1212,7 +1212,7 @@ int issignal(p)
 				return (signum);
 			/*NOTREACHED*/
 
-		case SIG_IGN:
+		case (integer_t)SIG_IGN:
 			/*
 			 * Masking above should prevent us ever trying
 			 * to take action on an ignored signal other
diff --git a/server/kern/subr_prf.c b/server/kern/subr_prf.c
index 403d6e9..a3c5516 100644
--- a/server/kern/subr_prf.c
+++ b/server/kern/subr_prf.c
@@ -163,8 +163,28 @@ void panic(const char *fmt, ...)
 	else
 		panicstr = fmt;
 
+	/*
+	 * Print the format string itself before attempting to expand it.
+	 *
+	 * "%r" re-expands a format string against a va_list, and on this
+	 * build it produces garbage: the panic text varied between
+	 * otherwise identical boots ("UWVS+" in one build, "UWVS\002k" in
+	 * the next), which means it is printing whatever memory it lands
+	 * on rather than a message. Two printf implementations are linked
+	 * here -- this file's and libsa_mach's -- and -z muldefs makes
+	 * the choice depend on link order, so which one handles %r is not
+	 * obvious.
+	 *
+	 * An unreadable panic message is worse than an unformatted one:
+	 * several panics are reachable at the same point in boot and
+	 * without the text there is no way to tell which fired. Printing
+	 * fmt with %s always identifies the panic, and the %r attempt is
+	 * kept after it so the arguments still appear when it works.
+	 */
+	printf("panic: %s\n", fmt);
+
 	va_start(ap, fmt);
-	printf("panic: %r\n", fmt, ap);
+	printf("panic args: %r\n", fmt, ap);
 	va_end(ap);
 
 #if	SECOND_SERVER
diff --git a/server/kern/vnode_if.sh b/server/kern/vnode_if.sh
index 7a680a9..ad11d2c 100644
--- a/server/kern/vnode_if.sh
+++ b/server/kern/vnode_if.sh
@@ -184,7 +184,12 @@ struct vnodeop_desc vop_default_desc = {
 END_OF_LEADING_COMMENT
 
 # Awk script to take vnode_if.src and turn it into vnode_if.c.
-$AWK 'function kill_surrounding_ws (s) {
+$AWK 'function bail (msg) {
+		print "vnode_if.sh: " msg > "/dev/stderr";
+		exit 1;
+	}
+
+	function kill_surrounding_ws (s) {
 		sub (/^[ \t]*/, "", s);
 		sub (/[ \t]*$/, "", s);
 		return s;
diff --git a/server/serv/serv_syscalls.c b/server/serv/serv_syscalls.c
index a0798b7..6e9f6cf 100644
--- a/server/serv/serv_syscalls.c
+++ b/server/serv/serv_syscalls.c
@@ -540,14 +540,14 @@ boolean_t thread_signal(struct proc *p, thread_t thread, int sig, int code)
 	}
 
 	switch ((vm_offset_t)p->p_sigacts->ps_sigact[sig]) {
-	    case SIG_IGN:
-	    case SIG_HOLD:
+	    case (integer_t)SIG_IGN:
+	    case (integer_t)SIG_HOLD:
 		/*
 		 * Should not get here unless traced.
 		 */
 		break;
 
-	    case SIG_DFL:
+	    case (integer_t)SIG_DFL:
 		sig_default(p, sig);
 		break;
 
@@ -697,15 +697,15 @@ bsd_take_signal(
 	p->p_siglist &= ~sigmask(sig);
 	action = ps->ps_sigact[sig];
 	switch ((vm_offset_t)action) {
-	    case SIG_IGN:
-	    case SIG_HOLD:
+	    case (integer_t)SIG_IGN:
+	    case (integer_t)SIG_HOLD:
 		/*
 		 * Should not get here.
 		 */
 		sig = 0;
 		break;
 
-	    case SIG_DFL:
+	    case (integer_t)SIG_DFL:
 		/*
 		 * take default action
 		 */
diff --git a/server/serv/server_init.c b/server/serv/server_init.c
index 25907d4..3ad6e20 100644
--- a/server/serv/server_init.c
+++ b/server/serv/server_init.c
@@ -298,7 +298,22 @@ void init_ports()
 				       &default_processor_set);
 }
 
-char default_root[] = "hd0a"; /* XXX */
+/*
+ * OSFMK 7.3's hd driver builds its partition table in getvtoc() by
+ * reading sector 0 as a DOS/BIOS partition table. When that fails --
+ * which it does on an unpartitioned image -- it falls back to
+ *
+ *     "make partition 'c' the whole disk in case of failure"
+ *
+ * setting d_partitions[PART_DISK], and PART_DISK is 2, which
+ * dev_name_lookup spells 'c' since it maps partition letters a-h onto
+ * indices 0-7.
+ *
+ * So "hd0c" is the whole of an unpartitioned disk and needs no MBR,
+ * no BSD disklabel and no partition arithmetic. "hd0a" would require
+ * a real DOS partition table.
+ */
+char default_root[] = "hd0c";
 
 void parse_arguments(
 	int	argc,
@@ -693,10 +708,35 @@ void main(
 #if OSFMACH3
 kernel_boot_info_t kernel_boot_info;
 
+/*
+ * The compiled-in configuration, used when a server starts with
+ * argc == 0 -- which is always here, since the bootstrap task gives
+ * servers a deliberate dummy zero argument count.
+ *
+ * get_config_info() below hands this to parse_arguments() as argc 4, so
+ * these four strings ARE the configuration; default_root[] above is not
+ * consulted on this path. Changing default_root alone has no effect,
+ * which cost a round to discover: a hardware breakpoint on
+ * device_open() showed LITES asking the kernel for "hd0a" long after
+ * default_root had been changed to "hd0c".
+ *
+ * hd0a -> hd0c, twice. Partition 'c' is the whole disk: getvtoc() in
+ * the kernel's hd driver builds its partition table by reading sector 0
+ * as a DOS partition table, and when that fails -- as it does on an
+ * unpartitioned image -- it falls back to "make partition 'c' the whole
+ * disk", setting d_partitions[PART_DISK] with PART_DISK 2, which
+ * dev_name_lookup spells 'c'. Partition 'a' simply does not exist
+ * there, so hdopen() returns D_NO_SUCH_DEVICE (2502, seen as
+ * kr = 0x9c6 in the "cannot mount root" panic).
+ *
+ * hd0f in the first entry is the server directory prefix and is left
+ * alone: it is only used to derive a path when none is given, and
+ * entry 3 supplies that here.
+ */
 char argv_space[10][40] = {"/dev/hd0f/mach_servers/startup", 
 			   "-s",
-			   "hd0a",
-			   "/dev/hd0a/mach_servers",
+			   "hd0c",
+			   "/dev/hd0c/mach_servers",
 			   (char *)0,};
 
 void get_config_info(
diff --git a/server/serv/user_copy.c b/server/serv/user_copy.c
index b23cb19..327e9f0 100644
--- a/server/serv/user_copy.c
+++ b/server/serv/user_copy.c
@@ -211,8 +211,19 @@ copyinstr(from, to, max_len, len_copied)
 	    cp = data + ((vm_offset_t)from - start);
 	    while (count < cur_max) {
 		count++;
-		if ((*((char *)to)++ = *cp++) == 0) {
-		    goto done;
+		/*
+		 * Was: if ((*((char *)to)++ = *cp++) == 0)
+		 * A cast is not an lvalue in modern C, so the
+		 * post-increment is spelled out. Semantics unchanged:
+		 * copy one byte, advance both pointers, stop at NUL.
+		 */
+		{
+		    char _c = *cp++;
+
+		    *(char *)to = _c;
+		    to = (char *)to + 1;
+		    if (_c == 0)
+			goto done;
 		}
 	    }
 	    (void) vm_deallocate(mach_task_self(),
diff --git a/server/serv/vn_pager_misc.c b/server/serv/vn_pager_misc.c
index 30d9f2c..7c1a9d0 100644
--- a/server/serv/vn_pager_misc.c
+++ b/server/serv/vn_pager_misc.c
@@ -393,7 +393,13 @@ void vnode_pager_uncache(struct vnode *vn)
 	behavior.copy_strategy = pager->copy_strategy;
 	behavior.temporary = FALSE;
 	behavior.invalidate = FALSE;
-	behavior.write_completions = FALSE;
+	/*
+	 * OSFMK 7.3 replaced write_completions in
+	 * struct memory_object_behave_info with silent_overwrite
+	 * and advisory_pageout. Set what exists.
+	 */
+	behavior.silent_overwrite = FALSE;
+	behavior.advisory_pageout = FALSE;
 	kr = memory_object_change_attributes(pager->control_port,
 					     MEMORY_OBJECT_BEHAVIOR_INFO,
 					     (memory_object_info_t) &behavior,
diff --git a/server/serv/xmm_interface.c b/server/serv/xmm_interface.c
index 4f92d09..55ab08b 100644
--- a/server/serv/xmm_interface.c
+++ b/server/serv/xmm_interface.c
@@ -106,27 +106,106 @@ kern_return_t seqnos_memory_object_notify (
 	/* move port from waiting set to ux server set */
 	ux_server_add_port(memory_object);
 
-	attributes.object_ready  = TRUE;
-	attributes.copy_strategy = pager->copy_strategy;
-	attributes.may_cache     = pager->may_cache;
-
-	kr = memory_object_establish(host_name,
-				     memory_object,
-#if OSF_LEDGERS
-				     MACH_PORT_NULL,
-#endif /* OSF_LEDGERS */
-				     memory_control,
-				     memory_object,
-				     VM_PROT_ALL,
-				     security_id,
-				     MEMORY_OBJECT_ATTRIBUTE_INFO,
-				     (memory_object_info_t) &attributes,
-				     MEMORY_OBJECT_ATTR_INFO_COUNT);
+	/*
+	 * OSFMK 7.3's struct memory_object_attr_info is
+	 * copy_strategy, cluster_size, may_cache_object, temporary.
+	 * object_ready has no counterpart; may_cache was renamed.
+	 */
+	attributes.copy_strategy    = pager->copy_strategy;
+	attributes.may_cache_object = pager->may_cache;
+
+	/*
+	 * memory_object_establish was a NORMA routine and OSFMK 7.3
+	 * removed it (mach.defs:247 keeps the id as a skip). This
+	 * handler belongs to the NORMA notify protocol, which this
+	 * kernel never initiates -- Smem_svr does not reference
+	 * seqnos_memory_object_notify at all -- so the call is
+	 * unreachable here. The attribute setting that establish used
+	 * to carry now happens in seqnos_memory_object_init above, via
+	 * memory_object_change_attributes.
+	 */
+	panic("seqnos_memory_object_notify: NORMA protocol not supported");
+	kr = KERN_FAILURE;
 	assert(kr == KERN_SUCCESS);
 	DEBUG_PRINT(("seqnos_memory_object_notify(x%x) end\n", memory_object));
 	XMM_END_SERVER();
 }
 
+/*
+ * OSFMK 7.3 has neither memory_object_establish (a NORMA routine; see
+ * mach.defs:247, "skip; was memory_object_establish") nor
+ * memory_object_ready (mach.defs:864, "was skip; memory_object_ready").
+ * Both were folded into memory_object_change_attributes, where setting
+ * the attributes is itself what declares the object ready.
+ *
+ * This follows MkLinux's inode_object_init in
+ * mklinux/src/osfmach3/server/inode_pager.c, which is written against
+ * this same kernel -- our osfmk is a copy of MkLinux's -- and whose
+ * comment on the call reads "tell the micro-kernel that the memory
+ * object is ready on our side".
+ */
+kern_return_t seqnos_memory_object_init (
+	mach_port_t memory_object,
+	mach_port_seqno_t seqno,
+	mach_port_t memory_control,
+	mach_port_t memory_object_name,
+	vm_size_t memory_object_page_size)
+{
+	struct vnode *vn;
+	vn_pager_t pager;
+	kern_return_t kr;
+	memory_object_attr_info_data_t attributes;
+
+	XMM_START_SERVER();
+	DEBUG_PRINT(("seqnos_memory_object_init(x%x) start\n", memory_object));
+	vn = port_to_vnode_lookup(memory_object, seqno);
+	if (!vn)
+	    panic("seqnos_memory_object_init no vn");
+	unix_master();		/* XXX */
+	pager = vn->v_vmdata;
+	if (!pager)
+	    panic("seqnos_memory_object_init no pager");
+	vref(vn);		/* the mapping is one reference */
+	assert(pager->control_port == MACH_PORT_NULL);
+	pager->control_port = memory_control;
+	pager->name_port = memory_object_name;
+	pager->page_size = memory_object_page_size;
+
+	/* move port from waiting set to ux server set */
+	ux_server_add_port(memory_object);
+
+	attributes.copy_strategy    = pager->copy_strategy;
+	attributes.cluster_size     = 0;	/* default cluster size */
+	attributes.may_cache_object = pager->may_cache;
+	attributes.temporary        = FALSE;
+	kr = memory_object_change_attributes(memory_control,
+					     MEMORY_OBJECT_ATTRIBUTE_INFO,
+					     (memory_object_info_t) &attributes,
+					     MEMORY_OBJECT_ATTR_INFO_COUNT,
+					     MACH_PORT_NULL);
+	assert(kr == KERN_SUCCESS);
+	DEBUG_PRINT(("seqnos_memory_object_init(x%x) end\n", memory_object));
+	XMM_END_SERVER();
+}
+
+/*
+ * Part of the NORMA discard protocol, which this kernel never
+ * initiates -- it has no norma/ directory at all. Smem_svr references
+ * the handler only because the message id is still reserved in the
+ * .defs. MkLinux's inode_object_discard_request is likewise a panic.
+ */
+kern_return_t seqnos_memory_object_discard_request (
+	mach_port_t memory_object,
+	mach_port_seqno_t seqno,
+	mach_port_t memory_control,
+	vm_offset_t offset,
+	vm_size_t length,
+	mach_port_t reply_to)
+{
+	panic("seqnos_memory_object_discard_request");
+	return KERN_FAILURE;
+}
+
 #else /* OSFMACH3 */
 
 kern_return_t seqnos_memory_object_init (
diff --git a/server/ufs/ext2fs/i386-bitops.h b/server/ufs/ext2fs/i386-bitops.h
index a66679e..8f10802 100644
--- a/server/ufs/ext2fs/i386-bitops.h
+++ b/server/ufs/ext2fs/i386-bitops.h
@@ -75,24 +75,46 @@ extern __inline__ int test_bit(int nr, void * addr)
 extern inline int find_first_zero_bit(void * addr, unsigned size)
 {
 	int res;
+	/*
+	 * d0, d1, d2 exist only to declare ECX, EDI and EAX as
+	 * read-write operands; see the constraint note below.
+	 */
+	int d0, d1, d2;
 
 	if (!size)
 		return 0;
-	__asm__("
-		cld
-		movl $-1,%%eax
-		xorl %%edx,%%edx
-		repe; scasl
-		je 1f
-		xorl -4(%%edi),%%eax
-		subl $4,%%edi
-		bsfl %%eax,%%edx
-1:		subl %%ebx,%%edi
-		shll $3,%%edi
+	__asm__("\n\
+		cld\n\
+		movl $-1,%%eax\n\
+		xorl %%edx,%%edx\n\
+		repe; scasl\n\
+		je 1f\n\
+		xorl -4(%%edi),%%eax\n\
+		subl $4,%%edi\n\
+		bsfl %%eax,%%edx\n\
+1:		subl %%ebx,%%edi\n\
+		shll $3,%%edi\n\
 		addl %%edi,%%edx"
-		:"=d" (res)
-		:"c" ((size + 31) >> 5), "D" (addr), "b" (addr)
-		:"ax", "cx", "di");
+		/*
+		 * The original constraints named ECX and EDI as inputs
+		 * ("c" and "D") *and* as clobbers ("cx", "di"). That is
+		 * invalid: a register cannot be clobbered when the
+		 * compiler must also set it up as an input, because the
+		 * value has to survive until the asm reads it. The block
+		 * really does modify both -- repe decrements ECX and
+		 * scasl advances EDI -- so they are read-write operands,
+		 * spelled as early-clobber outputs tied to matching
+		 * inputs. EAX is written by the movl and so is an output
+		 * too, rather than a clobber.
+		 *
+		 * Older GCC tolerated the original form. Modern GCC
+		 * rejects it at -O2 with "asm operand has impossible
+		 * constraints or there are not enough registers", while
+		 * accepting it at -O0, which is the signature of a
+		 * constraint bug rather than genuine register pressure.
+		 */
+		:"=d" (res), "=&c" (d0), "=&D" (d1), "=&a" (d2)
+		:"1" ((size + 31) >> 5), "2" (addr), "b" (addr));
 	return res;
 }
 
@@ -105,10 +127,10 @@ extern inline int find_next_zero_bit (void * addr, int size, int offset)
 		/*
 		 * Look for zero in first byte
 		 */
-		__asm__("
-			bsfl %1,%0
-			jne 1f
-			movl $32, %0
+		__asm__("\n\
+			bsfl %1,%0\n\
+			jne 1f\n\
+			movl $32, %0\n\
 1:			"
 			: "=r" (set)
 			: "r" (~(*p >> bit)));
@@ -146,10 +168,10 @@ extern inline char * memscan(void * addr, unsigned char c, int size)
 {
         if (!size)
                 return addr;
-        __asm__("cld
-                repnz; scasb
-                jnz 1f
-                dec %%edi
+        __asm__("cld\n\
+                repnz; scasb\n\
+                jnz 1f\n\
+                dec %%edi\n\
 1:              "
                 : "=D" (addr), "=c" (size)
                 : "0" (addr), "1" (size), "a" (c));
diff --git a/include/i386/param.h b/include/i386/param.h
index 3aa467d..8ab6921 100644
--- a/include/i386/param.h
+++ b/include/i386/param.h
@@ -155,7 +155,29 @@
  * Base address for U*X system call emulator.
  */
 #define	EMULATOR_BASE	0xa0000000
-#define	EMULATOR_END	0xa0040000
+/*
+ * The emulator's window, and at the top of it the four shared pages
+ * (emul_mapped.c: shared_readwrite at END-4 pages, shared_base_rw at
+ * END-2, shared_base_ro at END-1; mapin_user() maps them there).
+ *
+ * END was 0xa0040000, giving a 256 KB window. Our emulator's image is
+ * 249931 bytes and so runs to 0xa003d04b, while the shared region
+ * starts at END-4*0x1000 = 0xa003c000: an overlap of 4171 bytes. The
+ * emulator's own data and bss sat on top of the shared pages, so
+ * us_version read as garbage, the emulator disabled the shared region
+ * ("shared region mismatch b/1"), and the structures LITES keeps there
+ * -- us_vmspace, us_limit -- were read as pointers, which is why the
+ * first program died with a protection fault at an address that moved
+ * between runs.
+ *
+ * Widening the window to 512 KB moves the shared pages clear of the
+ * image without moving EMULATOR_BASE, so the emulator's link address is
+ * unchanged. Nothing else on i386 depends on END's exact value: it is
+ * used by mapin_user() and emul_mapped.c, which both follow it, and by
+ * three range checks in i386/e_machinedep.c that ask only whether the
+ * PC lies inside the emulator.
+ */
+#define	EMULATOR_END	0xa0080000
 				/* 256 Kbytes */
 
 /*
diff --git a/server/kern/init_main.c b/server/kern/init_main.c
index 14575d7..d2964c3 100644
--- a/server/kern/init_main.c
+++ b/server/kern/init_main.c
@@ -254,6 +254,7 @@ noreturn system_setup()
 #if	MAP_UAREA
 	p->p_vmspace = &p->p_shared_rw->us_vmspace;
 	p->p_limit = &p->p_shared_ro->us_limit;
+
 #else
 	p->p_vmspace = &p->p_realvmspace;
 	p->p_limit = &limit0;
@@ -359,8 +360,25 @@ noreturn system_setup()
 	/* Mount the root file system. */
 	kr = (*mountroot)();
 #if EXT2FS
-	/* XXX if FFS fails, fall back to EXT2FS */
-	if (kr == EINVAL) {
+	/*
+	 * XXX if FFS fails, fall back to EXT2FS
+	 *
+	 * EIO as well as EINVAL. ffs_mountfs() returns EIO when the
+	 * superblock magic is wrong -- ffs_vfsops.c marks that return
+	 * "XXX needs translation" -- so on a disk holding an ext2
+	 * filesystem the fallback was never reached and ext2_mountroot()
+	 * never ran. The symptom is a "cannot mount root" panic with no
+	 * ext2 diagnostic at all, even though ext2_vfsops.c would have
+	 * printed one had it been called.
+	 *
+	 * The same file is inconsistent about which errno means "this is
+	 * not my filesystem": ffs_vfsops.c:214 sets EINVAL with the same
+	 * "needs translation" comment, and :261 returns EINVAL outright.
+	 * Widening the test here rather than changing what FFS returns
+	 * keeps the policy in the caller, where it belongs, and leaves
+	 * FFS's behaviour alone for anything else that depends on it.
+	 */
+	if (kr == EINVAL || kr == EIO) {
 		extern int ext2_mountroot __P((void));
 
 		kr = ext2_mountroot();
@@ -393,6 +411,59 @@ noreturn system_setup()
 
 	initproc = newproc(p, TRUE, FALSE);
 
+	/*
+	 * Give initproc a shared region.
+	 *
+	 * newproc() allocates the proc but does not set the shared
+	 * region up; kern_fork() does that itself afterwards, under
+	 * MAP_UAREA in serv_fork.c, by calling mapin_user() and then
+	 * stamping us_version, pointing us_proc_pointer at the new proc
+	 * and initialising the two share locks.
+	 *
+	 * initproc is made by calling newproc() directly, so none of
+	 * that ran, and us_version held whatever the page contained.
+	 * The emulator checks that stamp before it will use the region
+	 * (emul_mapped.c):
+	 *
+	 *	if (shared_base_ro->us_version != USHARED_VERSION) {
+	 *		e_emulator_error("shared region mismatch %x/%x", ...);
+	 *		return;
+	 *	}
+	 *	shared_enabled = 1;
+	 *
+	 * so it printed "shared region mismatch b/1" and ran the first
+	 * program with shared_enabled at 0 -- the one process with no
+	 * parent to inherit a good region from.
+	 */
+	/*
+	 * initproc's parent.
+	 *
+	 * kern_fork() sets p2->p_pptr = p1 (serv_fork.c:479) after
+	 * calling newproc(). initproc is made by calling newproc()
+	 * directly, so p_pptr was never set.
+	 *
+	 * getpid() dereferences it, because COMPAT_43 is on in this
+	 * build (the generated compat_43.h has #define COMPAT_43 1):
+	 *
+	 *	*retval = p->p_pid;
+	 *  #if COMPAT_43 || defined(COMPAT_SUNOS)
+	 *	retval[1] = p->p_pptr->p_pid;
+	 *  #endif
+	 *
+	 * proc0 is the right parent: it is what init_main passes to
+	 * newproc() as p1, and what kern_fork() would have recorded.
+	 */
+	initproc->p_pptr = p;
+
+#if	MAP_UAREA
+	initproc->p_shared_rw->us_inuse = 0;
+	initproc->p_shared_rw->us_debug = 0;
+	share_lock_init(&initproc->p_shared_rw->us_lock);
+	share_lock_init(&initproc->p_shared_rw->us_siglock);
+	initproc->p_shared_ro->us_version = USHARED_VERSION;
+	initproc->p_shared_ro->us_proc_pointer = (vm_offset_t)initproc;
+#endif	/* MAP_UAREA */
+
 	/* exception handler */
 	exception_port = ux_handler_setup();
 #if OSFMACH3
diff --git a/include/sys/elf.h b/include/sys/elf.h
index 0d6257f..7c11ee6 100644
--- a/include/sys/elf.h
+++ b/include/sys/elf.h
@@ -80,7 +80,23 @@ typedef struct {
 
    Also places a ceiling on the number of program headers.  */
 
-#define MAX_PHDRS 4
+/*
+ * Raised from 4.
+ *
+ * Four was enough for the ELF a 1995 linker produced. Modern GNU ld
+ * emits GNU_STACK and GNU_RELRO as well, so the emulator alone has six
+ * program headers, and parse_exec_file() loops to e_phnum over this
+ * array. Reading past it fed garbage into the loader: the real data and
+ * bss segment set li->zero_start correctly and an out-of-bounds header
+ * that happened to look like a writable PT_LOAD then overwrote it, so
+ * the emulator's crt0 cleared the wrong page and left its BSS holding
+ * whatever was there.
+ *
+ * parse_exec_file() also bounds its loop by this value now, so a binary
+ * with more headers than this loses segments rather than reading past
+ * the end of the struct. Sixteen is generous for the linkers in use.
+ */
+#define MAX_PHDRS 16
 typedef struct {
   Elf32_Ehdr ehdr;
   Elf32_Phdr phdrs[MAX_PHDRS];
diff --git a/liblites/exec_file.c b/liblites/exec_file.c
index b30c671..b422edc 100644
--- a/liblites/exec_file.c
+++ b/liblites/exec_file.c
@@ -582,14 +582,29 @@ mach_error_t elf_parse_exec_file(
 {
 	unsigned int phdr;
 
-	if (nsecs < elf->ehdr.e_phnum)
+	/*
+	 * The loop writes secs[phdr+1].how = EXEC_M_STOP after the last
+	 * segment, so the array needs one entry more than the number of
+	 * headers processed, not the same number. The old test was
+	 * "nsecs < e_phnum", which let a 6-header binary into a 6-entry
+	 * array and wrote one past the end.
+	 */
+	if (nsecs <= elf->ehdr.e_phnum && nsecs <= MAX_PHDRS)
 		return LITES_EFBIG;
 
 	exec_load_info_clear(li);
 
 
 	/* Now iterate over them filling in info as needed.  */
-	for (phdr = 0; phdr < elf->ehdr.e_phnum; phdr++) {
+	/*
+	 * Bounded by MAX_PHDRS as well as e_phnum: elf->phdrs is a
+	 * fixed-size array overlaid on the file, and looping to e_phnum
+	 * alone reads past it for any binary with more headers. See
+	 * include/sys/elf.h.
+	 */
+	for (phdr = 0;
+	     phdr < elf->ehdr.e_phnum && phdr < MAX_PHDRS;
+	     phdr++) {
 		vm_size_t off;
 
 		/* Skip zero sized segments and those which are not part
diff --git a/emulator/emul_exec.c b/emulator/emul_exec.c
index 3875119..1956eed 100644
--- a/emulator/emul_exec.c
+++ b/emulator/emul_exec.c
@@ -234,7 +234,12 @@ errno_t emul_exec_load(
 	vm_offset_t		*heap_end,	/* OUT */
 	mach_port_t		image_port)
 {
-#define NSECTIONS 6		/* generous: 4 is needed for known binaries */
+/*
+ * One more than MAX_PHDRS: parse_exec_file() writes an EXEC_M_STOP
+ * entry after the last segment. Was 6, which was one too few for a
+ * six-header binary.
+ */
+#define NSECTIONS (MAX_PHDRS + 1)
 	mach_error_t kr;
 	struct exec_section secs[NSECTIONS];
 	int i;
diff --git a/server/serv/server_exec.c b/server/serv/server_exec.c
index 0fedb29..46b259b 100644
--- a/server/serv/server_exec.c
+++ b/server/serv/server_exec.c
@@ -870,7 +870,12 @@ static mach_error_t server_exec_load(
 	union exec_data		*hdr,
 	struct exec_load_info	*li)	/* OUT (space allocated by caller) */
 {
-#define NSECTIONS 6		/* generous: 4 is needed for known binaries */
+/*
+ * One more than MAX_PHDRS: parse_exec_file() writes an EXEC_M_STOP
+ * entry after the last segment. Was 6, which was one too few for a
+ * six-header binary such as the emulator.
+ */
+#define NSECTIONS (MAX_PHDRS + 1)
 	mach_error_t kr;
 	struct exec_section secs[NSECTIONS];
 	int i;
diff --git a/emulator/i386/e_machinedep.c b/emulator/i386/e_machinedep.c
index a74c4f7..a0e8a2a 100644
--- a/emulator/i386/e_machinedep.c
+++ b/emulator/i386/e_machinedep.c
@@ -49,6 +49,30 @@ e_fork_call(boolean_t isvfork, pid_t *pid, boolean_t *ischild)
 
 	volatile int x;
 
+	/*
+	 * emul_save_state() returns twice, like setjmp(): once in the
+	 * parent with 0, once in the child with the parent's pid. After
+	 * the second return, any local or parameter not declared
+	 * volatile has an indeterminate value -- the compiler may have
+	 * cached it in a register the restore does not reload, or have
+	 * considered its stack slot dead.
+	 *
+	 * Only x was volatile. pid and ischild are read on the child
+	 * path and are parameters, so they cannot be redeclared; they
+	 * are copied here, before the save, and used from these copies
+	 * afterwards.
+	 *
+	 * The two asm volatile statements bracketing the save were the
+	 * previous attempt at this. They clobber eax, edx and ecx only,
+	 * which sufficed for the compiler of 1995. A modern GCC reloaded
+	 * pid from its stack slot, which in the child held garbage, so
+	 * *ischild = TRUE succeeded and *pid = x faulted at an address
+	 * that varied between runs.
+	 */
+	pid_t * volatile	v_pid = pid;
+	boolean_t * volatile	v_ischild = ischild;
+	volatile boolean_t	v_isvfork = isvfork;
+
 	/*
 	 * Set up registers for child.  It resumes on its own stack.
 	 */
@@ -64,8 +88,8 @@ e_fork_call(boolean_t isvfork, pid_t *pid, boolean_t *ischild)
 		     : /* no outputs */
 		     : "eax", "edx", "ecx", "cc");
 	if (x != 0) {
-		*ischild = TRUE;
-		*pid = x;
+		*v_ischild = TRUE;
+		*v_pid = x;
 		child_init();
 		if (syscall_debug > 1)
 		    e_emulator_error("CHILD parent=%d", x);
@@ -79,14 +103,14 @@ e_fork_call(boolean_t isvfork, pid_t *pid, boolean_t *ischild)
 	/*
 	 * Create the child.
 	 */
-	error = (isvfork?bsd_vfork:bsd_fork)
+	error = (v_isvfork?bsd_vfork:bsd_fork)
 	    (our_bsd_server_port,
 	     (int *)&state,
 	     i386_THREAD_STATE_COUNT,
 	     rv);
 
 	if (error == 0)
-	    *pid = rv[0];
+	    *v_pid = rv[0];
 
 	*ischild = FALSE;
 	return error;
diff --git a/emulator/i386/emul_misc_asm.s b/emulator/i386/emul_misc_asm.s
index b334e7a..827df51 100644
--- a/emulator/i386/emul_misc_asm.s
+++ b/emulator/i386/emul_misc_asm.s
@@ -73,7 +73,21 @@ emul_save_state:
 	pushf			/* flags via stack */
 	popl	%ecx		/* ... */
 	movl	%ecx,56(%edx)	/* ... flags to state[14] */
-	movl	%esp,%ecx
+	/*
+	 * Save the esp the caller will have *after* this function
+	 * returns, not the one inside it.
+	 *
+	 * state[12] holds the return address, so a restore resumes at
+	 * the instruction after the call. But esp here still has that
+	 * return address on top of it; a real "ret" pops it and adds 4.
+	 * A restore does not, so the child resumed four bytes low and
+	 * every N(%esp) offset in the caller addressed the wrong slot.
+	 *
+	 * That is why e_fork_call's child path faulted on *pid = x while
+	 * the adjacent *ischild = TRUE succeeded: neighbouring slots,
+	 * one of which happened to hold something writable.
+	 */
+	leal	4(%esp),%ecx
 	movl	%ecx,60(%edx)	/* sp to state[15] */
 	mov	%ss,%ax
 	movl	%eax,64(%edx)	/* ss to state[16] */
diff --git a/server/serv/tty_io.c b/server/serv/tty_io.c
index 025aaaa..455c133 100644
--- a/server/serv/tty_io.c
+++ b/server/serv/tty_io.c
@@ -257,6 +257,32 @@ mach_error_t tty_open(dev_t dev, int flag, int devtype, struct proc *p)
 	    tp->t_cflag = TTYDEF_CFLAG;
 
 	      rc = tty_param(tp, &tp->t_termios);
+
+	      /*
+	       * A device that does not implement TTY_STATUS is not an
+	       * error. tty_param() ends with
+	       *
+	       *   error = device_set_status(tp->t_device_port, TTY_STATUS,
+	       *                             (int *)&ttstat, ttstat_count);
+	       *   return (error);
+	       *
+	       * and OSFMK's i386 console answers D_INVALID_OPERATION,
+	       * because the kd driver has no TTY_STATUS. That error was
+	       * returned raw from here, through cons_open() and
+	       * spec_open() to open(), where e_mach_error_to_errno() turns
+	       * it into ENOTTY -- so every open of /dev/console failed with
+	       * "Inappropriate ioctl for device" and init could never get a
+	       * console.
+	       *
+	       * tty_param() already ignores the matching device_get_status()
+	       * with a (void) cast, and the line below sets TS_CARR_ON with
+	       * the comment "should get from TTY_STATUS", so this code
+	       * already assumes the interface may be absent. Treat a set
+	       * that is refused the same way: the speeds and flags simply do
+	       * not apply to this device.
+	       */
+	      if (rc == D_INVALID_OPERATION)
+		  rc = D_SUCCESS;
 	      if (rc != D_SUCCESS)
 		return(rc);
 
